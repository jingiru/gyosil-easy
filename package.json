{
  "name": "gyosil-easy",
  "version": "0.1.0",
  "private": true,
  "description": "학교 내부망용 실시간 교실 호출 및 응답 시스템",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names server,client \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm start --workspace server",
    "test": "npm test --workspace server && npm run build --workspace client"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
